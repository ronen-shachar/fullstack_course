// arithmetic operators = operands (values, variables, etc...)
// operators(+ - * /)

let students = 30;

// students = students + 1;
// students = students - 1;
// students = students * 2;
// students = students / 2;



console.log(students);